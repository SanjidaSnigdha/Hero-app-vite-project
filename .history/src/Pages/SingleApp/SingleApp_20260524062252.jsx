import React, { use } from 'react';

const SingleApp = ({trendingApp}) => {
    console.log(tre)
   
    return (
        <div>
            Single app
        </div>
    );
};

export default SingleApp;